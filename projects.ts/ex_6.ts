// exercise 6 : stripping names:
//solve:

let personName: string = "\t Aqsa Saeed \n";

console.log( `Name with whit space` ,personName);

let strippedName: string = personName.trim();

console.log(`stripped Name :` , strippedName);