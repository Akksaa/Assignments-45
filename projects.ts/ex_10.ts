// exercise 10 : adding comments:
//solve:
 
let person: string = "\t Aqsa Saeed \n";

console.log(person); // it is the name with whitespace.

let stripped: string = person.trim();

console.log(stripped); //it is the name without white space or it is stripped name.